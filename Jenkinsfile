pipeline {
  agent {
    dockerfile {
      filename 'jenkins/node610.dockerfile'
    }
    
  }
  stages {
    stage('Build') {
      steps {
        sh 'echo "npm install !!!!!!!!!!!"'
      }
    }
  }
}